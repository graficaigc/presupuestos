// Punto de entrada para Vercel: cada request se maneja acá como función
// serverless, usando la misma app de Express que corre en un servidor normal.
module.exports = require("../server/index.js");
